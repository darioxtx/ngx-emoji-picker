name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci

      #- name: Run ESLint
      #  run: npm run lint

      - name: Build library
        run: npm run build

      #- name: Run unit tests
      #  run: npm run test:ci

      #- name: Build
      #  run: npm run build

      #- name: coverage
      #  uses: codecov/codecov-action@v4
      #  with:
      #    token: ${{ secrets.CODECOV_TOKEN }}
  publish:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref_name == 'master'
    permissions:
      contents: write # to be able to publish a GitHub release
      issues: write # to be able to comment on released issues
      pull-requests: write # to be able to comment on released pull requests
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: npm run build-prod
      - name: Create npm package tarball
        id: pack
        working-directory: dist/ngx-emoji-picker
        run: |
          FILE=$(npm pack)

          echo "file=$FILE" >> "$GITHUB_OUTPUT"
          echo "path=dist/ngx-emoji-picker/$FILE" >> "$GITHUB_OUTPUT"
      - name: Resolve package metadata
        id: pkg
        run: |
          NAME=$(node -p "require('./dist/ngx-emoji-picker/package.json').name")
          VERSION=$(node -p "require('./dist/ngx-emoji-picker/package.json').version")

          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release and upload tarball
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"

          gh release create "$TAG" \
            "${{ steps.pack.outputs.path }}" \
            --title "${{ steps.pkg.outputs.name }} ${{ steps.pkg.outputs.version }}" \
            --generate-notes
